"use client";

import { ExperienceShell } from "@/experiences/ExperienceShell";

// EMBER — Cinematic Dark Gourmet (the realistic ceiling, base of all).
// A-EMBER-BUILD deepens: hero frames + glaze + headline igniting + storyboard §7.4.
export default function EmberExperience() {
  return <ExperienceShell />;
}
