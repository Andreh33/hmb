"use client";

import { ExperienceShell } from "@/experiences/ExperienceShell";

// NOVA — Immersive Particle Showpiece. A-NOVA-BUILD: image→particles burst/
// reform, orbitable cloud, scroll-camera, volumetrics, "build your burger".
export default function NovaExperience() {
  return <ExperienceShell />;
}
