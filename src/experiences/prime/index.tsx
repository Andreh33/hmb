"use client";

import { ExperienceShell } from "@/experiences/ExperienceShell";

// PRIME — Editorial Luxury + WebGL Fluid. A-PRIME-BUILD: duotone displacement
// reveal, GPU fluid glaze, liquid lens cursor, shader section transitions.
export default function PrimeExperience() {
  return <ExperienceShell />;
}
