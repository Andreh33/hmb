"use client";

import { ExperienceShell } from "@/experiences/ExperienceShell";

// SMASH — Brutalist Neon Kinetic. A-SMASH-BUILD: smashing layers, screen shake,
// velocity skew, glitch/RGB-split, draggable stickers, sound, snap-scroll.
export default function SmashExperience() {
  return <ExperienceShell />;
}
